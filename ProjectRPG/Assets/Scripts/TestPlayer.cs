using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestPlayer : MonoBehaviour
{
    public SObject_Status status;
    // Start is called before the first frame update
    void Start()
    {
        status.agility = 10;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
